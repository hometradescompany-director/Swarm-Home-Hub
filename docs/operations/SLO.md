# Swarm Home Hub service-level objectives

Status: **targets only** until production telemetry exists.

When operated live, measure:
- platform-door availability;
- tool routing success/refusal/error rates;
- residence lifecycle transition failures;
- journal durability;
- Atlas boundary failures;
- p95/p99 transport latency.

Numerical targets should be adopted after representative live telemetry exists, not inferred from CI.
